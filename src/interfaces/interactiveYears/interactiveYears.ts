/**
 * Интерактивные года
 * @param resolution Разрешение
 * @param firstYear Первый год
 * @param secondYear Второй год
 */
interface InteractiveYears {
  resolution: string;
  firstYear: number;
  secondYear: number;
}

/**
 * Свойства места для компонента с годами
 * @param resolution Разрешение
 */
interface PlaceYears {
  resolution: string;
}
